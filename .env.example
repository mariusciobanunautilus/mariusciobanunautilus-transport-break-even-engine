# Copy the app-specific files instead:
# - frontend/.env.example -> frontend/.env
# - backend/.env.example  -> backend/.env
#
# Production backend requires:
# DATABASE_URL, CORS_ORIGIN, ADMIN_EMAIL, ADMIN_PASSWORD and WORKSPACE_NAME
