VITE_API_BASE=http://localhost:10000
# Production same-service deploys ignore VITE_API_BASE by default.
# Set this only when the frontend intentionally calls a separate backend domain:
# VITE_ALLOW_EXTERNAL_API=true
